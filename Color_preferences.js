//code 5
let colors = [];
for(let i = 0; i < 3; i++){ //loop until users inputs 3 colors
    let color = prompt("What is your favorite color?");
    colors.push(color);
    console.log(colors);
}